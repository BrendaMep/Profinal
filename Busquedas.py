

mapa = [[],]

def amplitud(mapa, inicio, destino):
    arbol = set(inicio)
    recorrido =(inicio,None)
    while len(arbol) != 0:
        ciudad = #sacar arbol()
        if ciudad == destino:
            return ruta
        for vecino in ciudad:
            if vecino not in recorrido:
                arbol.add(vecino)
                recorrido.add(vecino,ciudad)
            break
        break
    break



